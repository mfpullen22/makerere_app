import "package:flutter/material.dart";

class OrientationScreen extends StatelessWidget {
  const OrientationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Orientation Screen");
  }
}
